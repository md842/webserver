http {
  server {
    listen                8080 ssl;
    index                 small.html;
    root                  tests/inputs;

    location / {
      try_files $uri small.html; # React Router handles 404 by serving index
    }
  }
}